package cn.samir.online.ui.fragment;import android.os.Bundle;import android.support.annotation.Nullable;import android.support.v7.widget.GridLayoutManager;import android.support.v7.widget.RecyclerView;import android.view.View;import cn.samir.domains.model.BaseModel;import cn.samir.online.adapter.AllCategoriesAdapter;import cn.samir.online.adapter.BaseRecyclerAdapter;import cn.samir.online.adapter.decorations.LeftBottomDecoration;import cn.samir.online.http.converts.TypeProvider;/** * Created by xiaw on 2017/5/5 0005. */public class AllCategoriesFragment extends RecyclerFragment {    public static AllCategoriesFragment newInstance(String url) {        Bundle params = new Bundle();        params.putString(PARAM_URL, url);        params.putBoolean(PARAM_MASK_TOOLBAR,true);        AllCategoriesFragment rf = new AllCategoriesFragment();        rf.setArguments(params);        return rf;    }    @Override    public RecyclerView.LayoutManager getLayoutManager() {        mL.setSpanSizeLookup(spanSizeLookup);        return mL;    }    @Override    public void onViewCreated(View view, @Nullable Bundle savedInstanceState) {        super.onViewCreated(view, savedInstanceState);    }    @Override    protected RecyclerView.ItemDecoration getItemDecorations() {        return new LeftBottomDecoration();    }    GridLayoutManager mL = new GridLayoutManager(getActivity(), 2);    @Override    public BaseRecyclerAdapter<BaseModel> getAdapter() {        return new AllCategoriesAdapter();    }    private GridLayoutManager.SpanSizeLookup spanSizeLookup = new GridLayoutManager.SpanSizeLookup() {        @Override        public int getSpanSize(int position) {            int size = 2;            if (dataRecyclerViewAdapter.getItemCount() > 0) {                int type = dataRecyclerViewAdapter.getItemViewType(position);                if (type == TypeProvider.TYPE_squareCard) {                    size = 1;                } else {                    size = 2;                }            }            //LogUtil.e("position=" + position + ",size=" + size);            return size;        }    };}